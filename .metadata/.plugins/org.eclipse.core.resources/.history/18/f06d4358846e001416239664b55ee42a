public class Solution {
	
	public static void main(String arg[]){
		Solution sol = new Solution();
		String s = "absba";
		String p ="****as****";
		sol.isMatch(s, p);
	}
	
    public boolean isMatch(String s, String p) {
        int i,j;
        
        // pre-rpocessing for p
        j = 0;
        while(j < p.length()-1){
            if(p.charAt(j) == '*' && p.charAt(j+1) == '*'){
                p = p.substring(0,j)+p.substring(j+1,p.length());
            }else{
                j++;
            }
        }
        
        i = 0;
        j = 0;
        while(i < s.length() && j < p.length()){
            if(p.charAt(j) == '*' && j != p.length()-1){
                int end = i;
                for(;end < s.length();end++){
                    if(isMatch(s.substring(i,end), p.substring(j+1,p.length())))
                        return true;
                }
                return false;
            }else if(p.charAt(j) == '*'){ // the last char in p is *
                return true;
            }else if(s.charAt(i) == p.charAt(j) || p.charAt(j) == '?'){
                i++;
                j++;
            }else{
                return false;
            }
        }
        if(i < s.length()){
            return false;
        }else if(j < p.length()){
            for(;j < p.length();j++)
                if(p.charAt(j) != '*')
                    return false;
            return true;
        }else{
            return true;
        }
    }
}
